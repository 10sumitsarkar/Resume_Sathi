import UploadResume from './UploadResume';

export const metadata = {
  title: 'Upload Resume',
  description: 'Upload a resume and extract details into the resume builder.',
};

export default function UploadResumePage() {
  return <UploadResume />;
}
