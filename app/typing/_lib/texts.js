// Original practice sentences written for TypeForge — safe to use freely.
export const QUOTES = [
  "Discipline is choosing between what you want now and what you want most.",
  "Small steps taken daily add up to distances that surprise everyone, including you.",
  "A calm mind types faster than a rushed one, even when the rushed one tries harder.",
  "Every expert typist was once someone who could not find the letter A without looking.",
  "Speed is what happens naturally once your fingers stop asking your eyes for permission.",
  "The keyboard does not care how you feel today; it only cares what you press next.",
  "Good habits are built one unremarkable, boring repetition at a time.",
  "You do not rise to the level of your goals, you fall to the level of your practice.",
  "Focus on accuracy first, because speed built on mistakes just means faster mistakes.",
  "The best time to start practicing was yesterday; the second best time is right now.",
  "Consistency beats intensity when the finish line is measured in months, not minutes.",
  "A quiet room, a steady breath, and ten honest minutes can change a habit.",
];

export const CODE_SNIPPETS = [
  `function sum(a, b) {\n  return a + b;\n}`,
  `const users = data.filter((u) => u.active);`,
  `for (let i = 0; i < items.length; i++) {\n  console.log(items[i]);\n}`,
  `class Stack {\n  constructor() {\n    this.items = [];\n  }\n}`,
  `export default function Page() {\n  return <main>Hello</main>;\n}`,
  `const isEven = (n) => n % 2 === 0;`,
  `try {\n  await fetchData();\n} catch (err) {\n  console.error(err);\n}`,
  `SELECT name, age FROM users WHERE age > 18;`,
];
